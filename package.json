{
  "name": "vocard-store-fa",
  "version": "1.0.0",
  "description": "the Vocabulary Flashcard Store Function App",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2",
    "@azure/functions": "^4.0.0",
    "@azure/storage-blob": "^12.23.0"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "@types/uuid": "^10.0.0",
    "azure-functions-core-tools": "^4.0.5858",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
