
// export an arry of constants which can import directly in other files
const Constants = {
    DEFAULT_MODULE_PARTITION_KEY: 'DEFAULT',
    DEFAULT_DEVICE_PARTITION_KEY: 'DEFAULT',
};

export const { DEFAULT_MODULE_PARTITION_KEY, DEFAULT_DEVICE_PARTITION_KEY } = Constants;